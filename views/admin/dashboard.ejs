<!DOCTYPE html>
<html lang="zh-HK">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta name="color-scheme" content="light dark">
  <meta name="description" content="本平台提供台灣一番賞代購、代抽、代收與跨境轉送服務，所有抽選行為於台灣完成，香港地區僅提供商品領取與寄送。">
  <title>管理後台 - 台灣一番賞委託代抽</title>
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
  <header>
    <div class="container">
      <a href="/" class="site-logo">
        <img src="/img/mirai-3.png" alt="TMA台灣分部-未來飛翔">
      </a>
      <h1>管理後台</h1>
      <p>Ichiban Kuji Admin Dashboard</p>
      <div style="margin-top: 12px;">
        <a href="/" class="btn btn-secondary btn-sm">返回首頁</a>
        <a href="/logout" class="btn btn-secondary btn-sm">登出</a>
      </div>
    </div>
  </header>

  <main class="container">
    <div class="admin-header">
      <h2>代抽服務列表</h2>
      <div style="display:flex; gap:12px; flex-wrap:wrap; justify-content:flex-end;">
        <a href="/admin/users" class="btn btn-secondary">會員管理</a>
        <a href="/admin/codes" class="btn btn-primary">驗證碼管理</a>
        <button id="resetRafflesBtn" class="btn btn-warning">重置列表</button>
        <a href="/admin/create" class="btn btn-primary">建立新代抽服務</a>
      </div>
    </div>

    <% if (raffles.length === 0) { %>
      <div class="empty-state">
        <p>尚無任何代抽服務</p>
        <a href="/admin/create" class="btn btn-primary">建立第一個代抽服務</a>
      </div>
    <% } else { %>
      <div class="admin-table-wrapper">
        <table class="admin-table">
          <thead>
            <tr>
              <th>ID</th>
              <th>標題</th>
              <th>狀態</th>
              <th>盒子</th>
              <th>價格</th>
              <th>建立時間</th>
              <th>操作</th>
            </tr>
          </thead>
          <tbody>
            <% raffles.forEach(raffle => { %>
              <tr>
                <td><%= raffle.id %></td>
                <td><%= raffle.title %></td>
                <td>
                  <span class="status-badge <%= raffle.status %>">
                    <% if (raffle.status === 'active') { %>
                      開放中
                    <% } else if (raffle.status === 'closed') { %>
                      已關閉
                    <% } else if (raffle.status === 'completed') { %>
                      已完成
                    <% } else if (raffle.status === 'draft') { %>
                      草稿
                    <% } %>
                  </span>
                </td>
                <td><%= raffle.remaining_boxes %> / <%= raffle.total_boxes %></td>
                <td>HK$<%= formatMoney(raffle.price_per_box) %> / NT$<%= formatMoney(raffle.price_per_box * twdPerHkd, 0) %></td>
                <td><%= new Date(raffle.created_at).toLocaleString('zh-TW', { timeZone: 'Asia/Taipei' }) %></td>
                <td class="actions">
                  <a href="/raffle/<%= raffle.id %>" target="_blank" class="btn btn-sm btn-secondary">查看</a>
                  <a href="/admin/raffles/<%= raffle.id %>/codes" class="btn btn-sm btn-primary">驗證碼</a>
                  <% if (raffle.status === 'active') { %>
                    <button class="btn btn-sm btn-warning change-status" 
                            data-id="<%= raffle.id %>" 
                            data-status="closed">關閉</button>
                  <% } else if (raffle.status === 'closed' || raffle.status === 'draft') { %>
                    <button class="btn btn-sm btn-success change-status"
                            data-id="<%= raffle.id %>"
                            data-status="active">開啟</button>
                  <% } %>
                </td>
              </tr>
            <% }) %>
          </tbody>
        </table>
      </div>
    <% } %>
  </main>

  <footer>
    <div class="container">
      <div class="legal-footer" style="background: #f8f9fa; border-radius: 8px; padding: 16px; margin-bottom: 16px; text-align: left; font-size: 13px; line-height: 1.6; color: #495057;">
        <strong>【服務聲明】</strong><br>
        <ol style="margin: 8px 0 0 20px; padding: 0;">
          <li>本平台僅提供日本／台灣一番賞商品之代購及代抽服務。</li>
          <li>所有抽選行為及相關交易均於海外（台灣）由當地合作單位完成，香港僅作為資訊展示及代購安排之平台，不涉及任何抽獎、博彩或機會遊戲活動之經營或操作。</li>
          <li>由於相關抽選活動並非於香港進行，本平台並不構成香港法例下之獎券、抽獎或賭博業務。</li>
          <li>用戶於下單時，即表示已年滿18歲，明白及同意抽選行為於海外進行，接受抽選結果之隨機性及不可預測性。</li>
          <li>所有商品內容、款式及中獎機率，均以當地官方公佈為準。基於抽選性質，所有訂單一經確認恕不接受退款或更換。</li>
        </ol>
      </div>
      <p>&copy; <%= new Date().getFullYear() %> Ichiban Kuji Proxy Service</p>
      <p style="margin-top: 8px; opacity: 0.7;">Powered by TMA台灣分部-未來飛翔</p>
    </div>
  </footer>

  <script src="/js/app.js"></script>
  <script>
    document.addEventListener('DOMContentLoaded', () => {
      initAdminDashboard();
    });
  </script>
</body>
</html>
